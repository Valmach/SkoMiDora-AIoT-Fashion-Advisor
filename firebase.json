{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": [
    {
      "target": "wardrobe",
      "rules": "storage.rules"
    },
    {
      "target": "legacy",
      "rules": "storage.rules"
    }
  ]
}
